# 1.1.0 acceptance evidence

Same evidence discipline as `1.0.0-acceptance.md`: every claim below is an
actual command run during this task (Phase 6 Task 6, 2026-08-11), not an
assertion. All commands were run from the worktree root
(`/Users/Zoli/Sites/Angular-Advanced-Odontogram/.claude/worktrees/phase6-resync`).
This release resyncs the engine core + test corpus to
`react-advanced-odontogram` v2.4.0 (payload version 2.20), delivered across
Phase 6 Tasks 1-5; this task is the docs/version/evidence cut only — no
functional code changed here beyond the version-literal bumps and the
`angular.json` budget adjustment justified in §(e).

## (a) SVG-fingerprint / FHIR / roundtrip parity against the regenerated v2.4.0 goldens

**Claim:** the Angular render still fingerprints byte-identically against
the goldens — now the v2.4.0 goldens, regenerated upstream and re-copied
verbatim by Phase 6 Task 1 (not re-captured by this port).

**Command:**

```
npx vitest run projects/angular-advanced-odontogram/src/lib/core/__tests__/parity.test.ts --reporter=verbose
```

**Output:**

```
 ✓ ... SP2 parity oracle (current engine must match frozen goldens) > SVG fingerprints match 15138ms
 ✓ ... SP2 parity oracle (current engine must match frozen goldens) > FHIR bundles match 4ms
 ✓ ... SP2 parity oracle (current engine must match frozen goldens) > round-trips match 1ms
 ✓ ... registry catalog matches today's tables > one axis per FIELD_MAPPINGS row with identical finding code + value group + skipValue 1ms
 ✓ ... registry catalog matches today's tables > enum/set/restoration axis values equal their LOCAL_VALUE_MAPS group exactly 0ms

 Test Files  1 passed (1)
      Tests  5 passed (5)
```

This is the parity proof: `svg-fingerprints.json`/`fhir-golden.json`/
`roundtrip-golden.json` under `core/__tests__/parity/` are the v2.4.0-era
goldens (Task 1's copy inventory); this run is the live engine's render
compared against them, all green. These same fixtures are part of the
full corpus run in §(c) below.

## (b) Regenerated-golden note

The FHIR, roundtrip, and SVG-fingerprint golden fixtures were regenerated
**upstream** at `react-advanced-odontogram` commit `f9b45fc` (v2.4.0) and
copied verbatim into this repo by Phase 6 Task 1 — this port never ran its
own `PARITY_CAPTURE=1` capture pass against a live browser/React render;
the goldens are upstream's own v2.4.0 output. The payload-version stamp in
the roundtrip/FHIR goldens is `2.20`, matching every export surface in this
release (see the CHANGELOG's `2.19` → `2.20` note). §(a)'s green run above,
plus the full corpus run in §(c), is the evidence that this port's engine
(byte-identical to upstream except the five sanctioned deviations) still
reproduces those goldens exactly.

## (c) Full suite state

**Corpus (`npm run test:corpus`, plain Vitest — framework-free engine +
copied React-derived corpus, now including the 11 new v2.4.0 corpus
files):**

```
npm run test:corpus
```

```
 Test Files  111 passed | 1 skipped (112)
      Tests  1160 passed | 1 skipped (1161)
   Duration  20.10s
```

Matches the state recorded at the end of Phase 6 Task 5 (`progress.md`:
"SUITE FULLY GREEN: test:ng 786/786 ×3, corpus 1160/1161") — unchanged by
this task, since no core/component file was touched.

**Angular (`npm run test:ng`, `ng test` under Vitest, ×2 for flake check,
run fresh in this task after the version bumps):**

Run 1:

```
 Test Files  75 passed (75)
      Tests  786 passed (786)
   Duration  8.28s
```

Run 2:

```
 Test Files  75 passed (75)
      Tests  786 passed (786)
   Duration  8.03s
```

Identical pass counts both runs — no flake. (786 = the 760 baseline from
1.0.0 + 26 net new/changed specs from the v2.4.0 resync, matching Task 5's
final tally.)

## (d) Visual/behavior change carried into this release

`sp17-followups.spec.ts`'s "SADDLE_Y_FRACTION_LOWER" assertion now encodes
upstream's v2.4.0 re-anchoring (`1 - SADDLE_Y_FRACTION`, the true geometric
mirror, superseding the earlier recon-measured `0.19`) — green as part of
§(c)'s `test:ng` run. This moves the lower-arch bridge-saddle bar's
rendered vertical position; it is upstream's own decision (see
`bridgeOverlay.ts`'s own comment, quoted in Task 5's report), not a defect.
Flagged in the CHANGELOG's "Visual change" note for owner awareness.

## (e) `npm pack --dry-run` — fresh build

**Commands (run in this exact order in this task):**

```
rm -rf dist
npm run build:styles
npx ng build angular-advanced-odontogram
cd dist/angular-advanced-odontogram && npm pack --dry-run
```

**Output:**

```
npm notice 📦  angular-advanced-odontogram@1.1.0
npm notice Tarball Contents
npm notice 1.1kB LICENSE
npm notice 991B README.md
npm notice 6.4kB fesm2022/angular-advanced-odontogram-loader-CIDHSWEq.mjs
npm notice 12.9kB fesm2022/angular-advanced-odontogram-loader-CIDHSWEq.mjs.map
npm notice 483.2kB fesm2022/angular-advanced-odontogram-notoArabic-DIKquCZ2.mjs
npm notice 485.0kB fesm2022/angular-advanced-odontogram-notoArabic-DIKquCZ2.mjs.map
npm notice 499.7kB fesm2022/angular-advanced-odontogram-notoSC-Cp7voOxx.mjs
npm notice 501.3kB fesm2022/angular-advanced-odontogram-notoSC-Cp7voOxx.mjs.map
npm notice 75.0kB fesm2022/angular-advanced-odontogram-roboto-B6GNF0Nj.mjs
npm notice 76.3kB fesm2022/angular-advanced-odontogram-roboto-B6GNF0Nj.mjs.map
npm notice 2.5MB fesm2022/angular-advanced-odontogram.mjs
npm notice 3.4MB fesm2022/angular-advanced-odontogram.mjs.map
npm notice 1.0kB package.json
npm notice 57.3kB styles.css
npm notice 245.7kB types/angular-advanced-odontogram.d.ts
npm notice Tarball Details
npm notice name: angular-advanced-odontogram
npm notice version: 1.1.0
npm notice filename: angular-advanced-odontogram-1.1.0.tgz
npm notice package size: 2.6 MB
npm notice unpacked size: 8.3 MB
npm notice shasum: a692f70c4e91e08018d6ead1611bd2f6ecb7bf3b
npm notice total files: 15
```

**FESM growth from the font modules — confirmed:** at 1.0.0 there was a
single `fesm2022/angular-advanced-odontogram.mjs` (2.0MB) + its sourcemap,
7 files total. At 1.1.0, ng-packagr's bundler split the library's own
`import("./fonts/loader")` / `import("./notoArabic")` / `import("./notoSC")`
/ `import("./roboto")` dynamic imports (§2 of the design spec, Task 1's
copy inventory) into **4 additional FESM chunk files** (`*-loader-*.mjs`,
`*-notoArabic-*.mjs`, `*-notoSC-*.mjs`, `*-roboto-*.mjs`, each with its own
`.map`) alongside the grown main FESM (2.0MB → 2.5MB — the new registry/
persistence/iso3950/pluginSanitize/PDF-settings/collapsible-cards surface,
not font content, since the fonts are the separately-chunked files). Total
files: 7 → 15; unpacked size: 5.1MB → 8.3MB. README, LICENSE, `styles.css`,
`package.json`, and the typings file remain present as before — no dev/test
artifacts leaked into the tarball; the growth is entirely the expected
font-chunk split plus the resynced engine content.

## (f) Consumer resolution proof

**Setup:** a scratch dir under `/private/tmp` (outside the repo), with
`node_modules/angular-advanced-odontogram` symlinked to the freshly built
`dist/angular-advanced-odontogram`, then Node's ESM resolver queried for
both the package root and its `./styles.css` subpath export.

**Commands:**

```
SCRATCH=/private/tmp/aao-consumer-proof-$$
mkdir -p "$SCRATCH/node_modules"
ln -s <repo>/dist/angular-advanced-odontogram "$SCRATCH/node_modules/angular-advanced-odontogram"
cd "$SCRATCH"
node --input-type=module -e "console.log(import.meta.resolve('angular-advanced-odontogram')); console.log(import.meta.resolve('angular-advanced-odontogram/styles.css'))"
rm -rf "$SCRATCH"
```

**Output:**

```
file:///Users/Zoli/Sites/Angular-Advanced-Odontogram/.claude/worktrees/phase6-resync/dist/angular-advanced-odontogram/fesm2022/angular-advanced-odontogram.mjs
file:///Users/Zoli/Sites/Angular-Advanced-Odontogram/.claude/worktrees/phase6-resync/dist/angular-advanced-odontogram/styles.css
```

Both subpaths resolve correctly per the package's `exports` map, unchanged
by the font-chunk split (the chunk files are referenced by relative dynamic
`import()` from inside `angular-advanced-odontogram.mjs` itself, not via the
package's public `exports` map — consumers never import them directly).
Scratch dir removed after the run (confirmed via a follow-up `ls` failing
with "No such file or directory").

## (g) Demo bundle-size note (`npm run build:demo`)

**Claim to check (task brief):** the font modules are dynamic-imported —
verify the main bundle did **not** balloon from them; record any new lazy
chunks; adjust budgets only if the main bundle itself genuinely grew, with
justification.

**Command:**

```
npm run build:demo
```

**Output (chunk table):**

```
Initial chunk files | Names   |  Raw size | Estimated transfer size
main-VO73PGOY.js    | main    |   1.81 MB |               340.81 kB
styles-4NLBYOIT.css | styles  |  55.25 kB |                 8.85 kB
chunk-5SBP4E5E.js   | -       |  28.67 kB |                 9.72 kB
chunk-TQPZRHFO.js   | -       | 931 bytes |               931 bytes
                    | Initial total | 1.89 MB |            360.32 kB

Lazy chunk files    | Names                                           |  Raw size | Transfer
chunk-K6D3N5DZ.js   | angular-advanced-odontogram-notoSC-Cp7voOxx     | 499.45 kB | 280.75 kB
chunk-DXVKGCB3.js   | angular-advanced-odontogram-notoArabic-DIKquCZ2 | 482.80 kB | 105.59 kB
chunk-ZIPLL3XA.js   | jspdf-es-min                                    | 411.27 kB | 112.84 kB
chunk-GXAQDK3Q.js   | html2canvas                                     | 202.84 kB |  38.48 kB
chunk-XO35LTWQ.js   | index-es                                        | 159.05 kB |  46.95 kB
chunk-AAETTGCX.js   | angular-advanced-odontogram-roboto-B6GNF0Nj     |  72.80 kB |  38.31 kB
chunk-PPAR3BUP.js   | angular-advanced-odontogram-loader-CIDHSWEq     |   2.17 kB |     822 B
chunk-FK6H3RFT.js   | -                                                |  272 bytes | 272 B
chunk-CVECNAKB.js   | purify-es                                        |   85 bytes |  85 B
```

**Confirmed: the three new PDF-font modules (`notoSC`, `notoArabic`,
`roboto`) plus their `loader` are all in the Lazy chunk files table, not
the Initial chunk files table** — the main bundle did not balloon from
font payloads; `core/fonts/loader.ts`'s `await import("./notoArabic")` /
`await import("./notoSC")` / `await import("./roboto")` (confirmed in
source) are genuinely code-split by the build.

**But the main bundle did grow — measured, not assumed.** To separate "did
the resync grow the main bundle" from "is 1.89MB new," this task built the
**1.0.0 release commit** (`cc65c5b`) in an isolated `git worktree` (removed
after use) and ran the identical `build:demo` command there:

```
git worktree add --detach <scratch> cc65c5b
cd <scratch> && npm install --no-audit --no-fund
npx ng build angular-advanced-odontogram && npm run build:demo
```

**1.0.0 output (chunk table):**

```
Initial chunk files | Names         |  Raw size | Estimated transfer size
main-EYKRBC22.js    | main          |   1.48 MB |               227.93 kB
styles-SJPCLO54.css | styles        |  51.45 kB |                 8.33 kB
chunk-TQPZRHFO.js   | -             | 931 bytes |               931 bytes
                    | Initial total |   1.53 MB |               237.19 kB

Lazy chunk files    | Names         |  Raw size | Estimated transfer size
chunk-2Y4UNMW3.js   | jspdf-es-min  | 411.27 kB |               113.03 kB
chunk-GXAQDK3Q.js   | html2canvas   | 202.84 kB |                38.48 kB
chunk-XO35LTWQ.js   | index-es      | 159.05 kB |                46.95 kB
chunk-RAQLRN6X.js   | purify-es     |  28.70 kB |                 9.72 kB
chunk-FK6H3RFT.js   | -             | 272 bytes |               272 bytes
```

`bundle initial exceeded maximum budget. Budget 1.50 MB was not met by
29.12 kB with a total of 1.53 MB.` — **1.0.0 was already over the old
1.5MB warning budget** by 29KB (pre-existing, not a resync artifact); the
2MB error budget was untouched at both points in time, so both builds
succeeded either way.

**Delta:** main raw `1.48MB → 1.81MB` (+330KB raw / +112.88kB gzip
transfer). No font code is in this delta (fonts are new-in-1.1.0 lazy
chunks, absent entirely from 1.0.0's build). The growth is the v2.4.0
resync's new **always-loaded** engine surface: the new `registry/`
axis/FHIR entries, `fhir/iso3950.ts`, `persistence.ts`, `pluginSanitize.ts`
(and its `dompurify` dependency — note `purify-es`'s lazy-chunk size
dropped from 28.70kB at 1.0.0 to 85 bytes at 1.1.0, consistent with most of
its content moving from a lazy chunk into the main bundle now that
`pluginSanitize.ts` imports it synchronously from core, not behind a
dynamic import), the PDF-settings surface, collapsible-card state, and the
new availability-control wiring in the shell/settings-modal components —
all genuine new functionality shipped by Phase 6 Tasks 1-4, not bloat.

**Budget adjustment (justified by the measured delta above):**
`angular.json`'s `demo` production `initial` budget raised from
`maximumWarning: "1.5MB"` / `maximumError: "2MB"` to
`maximumWarning: "2MB"` / `maximumError: "2.5MB"` — same 500KB
warning-to-error gap as before, now with headroom above the measured
1.89MB actual (vs. the old budget's headroom having already been consumed
before this resync even started, per the 1.0.0 comparison above). Re-ran
`npm run build:demo` after the change: **no budget warning emitted**
(confirmed via `grep -i budget` on the full build log returning no
matches), build green.

## Full gate (all steps, same session, final state)

| Step | Command | Result |
|---|---|---|
| Corpus | `npm run test:corpus` | 111 passed + 1 skipped / 1160 passed + 1 skipped |
| Angular #1 | `npm run test:ng` | 75/75 files, 786/786 tests |
| Angular #2 | `npm run test:ng` | 75/75 files, 786/786 tests (identical — no flake) |
| Styles | `npm run build:styles` | Done in 202ms |
| Library | `npx ng build angular-advanced-odontogram` | Built angular-advanced-odontogram (1.7s) |
| Demo | `npm run build:demo` | Application bundle generation complete; no budget warning; `dist/demo` populated |

All green. Scope of this task: version-literal bumps
(`package.json` ×2, `package-lock.json`, `app-version.ts`'s `LIB_VERSION`
literal), `angular.json`'s demo budget adjustment (justified above), and
docs (`README.md`, `lang/README-hu.md`, `CHANGELOG.md`, the design spec's
§2/§10, this evidence sheet). No engine/component behavior was changed by
this task — all functional changes shipped in Phase 6 Tasks 1-5.
