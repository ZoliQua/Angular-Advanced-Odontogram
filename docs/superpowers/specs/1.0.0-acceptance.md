# 1.0.0 acceptance evidence

Maps every §7 ("Testing & parity proof") acceptance criterion in
`2026-08-06-angular-odontogram-port-design.md` to concrete evidence — an
actual command run during this task, not an assertion. All commands were run
from the worktree root
(`/Users/Zoli/Sites/Angular-Advanced-Odontogram/.claude/worktrees/phase5-release`)
on 2026-08-07/08.

## (a) SVG-fingerprint parity

**Claim:** the Angular render fingerprints byte-identically against the
frozen golden captured from React `react-advanced-odontogram` v2.2.0.

**Command:**

```
npx vitest run projects/angular-advanced-odontogram/src/lib/core/__tests__/parity.test.ts --reporter=verbose
```

**Output (relevant tail):**

```
✓ ... SP2 parity oracle (current engine must match frozen goldens) > SVG fingerprints match 15881ms
✓ ... SP2 parity oracle (current engine must match frozen goldens) > FHIR bundles match 3ms
✓ ... SP2 parity oracle (current engine must match frozen goldens) > round-trips match 6ms
✓ ... registry catalog matches today's tables > one axis per FIELD_MAPPINGS row with identical finding code + value group + skipValue 1ms
✓ ... registry catalog matches today's tables > enum/set/restoration axis values equal their LOCAL_VALUE_MAPS group exactly 0ms

 Test Files  1 passed (1)
      Tests  5 passed (5)
```

`parity.test.ts`'s "SVG fingerprints match" case loads
`parity/svg-fingerprints.json` (the frozen golden) and, for every case
produced by `parity/matrix.ts`'s `svgCases()`, renders the active layers via
`__renderActiveLayers` and asserts deep equality against the golden entry —
this is the byte-for-byte structural fingerprint comparison. `matrix.test.ts`
is the matrix-generation self-test (below) — both are part of the corpus-green
run in (d).

**`matrix.test.ts` (matrix-generation self-test) run:**

```
npx vitest run projects/angular-advanced-odontogram/src/lib/core/__tests__/parity/matrix.test.ts
```

```
 Test Files  1 passed (1)
      Tests  4 passed (4)
```

## (b) FHIR golden byte-identical

**Claim:** `buildFhirBundle` output is byte-identical to the FHIR golden
captured from the React source.

**Evidence:** same `parity.test.ts` run as (a) — "FHIR bundles match" (loads
`parity/fhir-golden.json`, asserts `buildFhirBundle(payload)` deep-equals the
golden bundle for every `payloadCases()` entry). Passed (see tail above).

`fhir.test.ts` (FHIR code-system coverage, not the golden byte-compare
itself) is corpus-green as part of (d)'s full run.

`parity/capture.test.ts` is the golden-*capture* harness (guarded behind
`PARITY_CAPTURE=1`, normally skipped so the frozen goldens are never
re-captured accidentally by the standard suite) — it is the origin of
`svg-fingerprints.json`/`fhir-golden.json`/`roundtrip-golden.json`, not a
comparison test; its "skipped" state is confirmed in the corpus run in (d)
(`1 skipped`).

## (c) Roundtrip golden (real React-2.2.0 export) imports + re-exports equal

**Claim:** a JSON/FHIR payload from React 2.2.0 parses and re-exports equal
(modulo version metadata).

**Evidence:** same `parity.test.ts` run — "round-trips match" (loads
`parity/roundtrip-golden.json`, asserts `parseFhirBundle(buildFhirBundle(payload))`
deep-equals the golden parsed result for every `payloadCases()` entry).
Passed (see tail above).

**Command:**

```
npx vitest run projects/angular-advanced-odontogram/src/lib/core/__tests__/payload-2-4-roundtrip.test.ts
```

**Output:**

```
 Test Files  1 passed (1)
      Tests  4 passed (4)
```

(`payload-2-4-roundtrip.test.ts` exercises the export/import round-trip of
the unified `cariesSeverity` field through `hydrateState`/FHIR build+parse —
corpus-green, not itself the golden-fixture comparison, which lives in
`parity.test.ts` above.)

## (d) Full suite state

**Corpus (`npm run test:corpus`, plain Vitest — framework-free engine +
copied React-derived corpus):**

```
npm run test:corpus
```

```
 Test Files  100 passed | 1 skipped (101)
      Tests  1061 passed | 1 skipped (1062)
   Duration  20.07s
```

Matches the baseline recorded at worktree start (`progress.md`: "corpus
100+1 green").

**Angular (`npm run test:ng`, `ng test` under Vitest, ×2 for flake check):**

Run 1:

```
 Test Files  74 passed (74)
      Tests  760 passed (760)
   Duration  13.97s
```

Run 2:

```
 Test Files  74 passed (74)
      Tests  760 passed (760)
   Duration  14.00s
```

Identical pass counts both runs — no flake.

## (e) `npm pack --dry-run`

**Commands:**

```
npm run build:styles
npx ng build angular-advanced-odontogram
cd dist/angular-advanced-odontogram && npm pack --dry-run
```

(Run after the 1.0.0 version bump, so the dist manifest reflects the
released version.)

**Output:**

```
npm notice 📦  angular-advanced-odontogram@1.0.0
npm notice Tarball Contents
npm notice 1.1kB LICENSE
npm notice 994B README.md
npm notice 2.0MB fesm2022/angular-advanced-odontogram.mjs
npm notice 2.9MB fesm2022/angular-advanced-odontogram.mjs.map
npm notice 992B package.json
npm notice 53.4kB styles.css
npm notice 125.3kB types/angular-advanced-odontogram.d.ts
npm notice Tarball Details
npm notice name: angular-advanced-odontogram
npm notice version: 1.0.0
npm notice filename: angular-advanced-odontogram-1.0.0.tgz
npm notice package size: 1.1 MB
npm notice unpacked size: 5.1 MB
npm notice total files: 7
```

README, LICENSE, `styles.css`, the FESM2022 bundle (+ sourcemap), typings
(`types/angular-advanced-odontogram.d.ts`), and `package.json` are all
present — 7 files total, no dev/test artifacts leaked into the tarball.

## (f) Consumer resolution proof

**Setup:** a scratch dir under `/private/tmp` (outside the repo), with
`node_modules/angular-advanced-odontogram` symlinked to the freshly built
`dist/angular-advanced-odontogram`, then Node's ESM resolver queried for both
the package root and its `./styles.css` subpath export.

**Commands:**

```
SCRATCH=/private/tmp/aao-consumer-proof-$$
mkdir -p "$SCRATCH/node_modules"
ln -s <repo>/dist/angular-advanced-odontogram "$SCRATCH/node_modules/angular-advanced-odontogram"
cd "$SCRATCH"
node --input-type=module -e "console.log(import.meta.resolve('angular-advanced-odontogram')); console.log(import.meta.resolve('angular-advanced-odontogram/styles.css'))"
cd / && rm -rf "$SCRATCH"
```

**Output:**

```
file:///Users/Zoli/Sites/Angular-Advanced-Odontogram/.claude/worktrees/phase5-release/dist/angular-advanced-odontogram/fesm2022/angular-advanced-odontogram.mjs
file:///Users/Zoli/Sites/Angular-Advanced-Odontogram/.claude/worktrees/phase5-release/dist/angular-advanced-odontogram/styles.css
```

Both subpaths resolve correctly per the package's `exports` map (`.` →
`fesm2022/angular-advanced-odontogram.mjs` via the `default` condition;
`./styles.css` → `styles.css` directly). Scratch dir removed after the run
(confirmed: a follow-up `ls` on the path failed with "No such file or
directory").

## (g) Known-drift note

Pinned to upstream `react-advanced-odontogram` v2.2.0 (payload version 2.19).
The upstream v2.2.1 resync — payload 2.20 (`patientDob`/`setPatientDob`,
individual notes, PDF-dialog DOB/split options, Plan-mode gating, bridge
lower-arch), core+corpus re-copy with the 3 sanctioned deviations reapplied,
`ExportOptionsModal` DOB field, and affected ported specs — is deferred to
**1.1.0**. Also riding with that resync: the `setReadOnly`-doesn't-notify
core question (the fix belongs upstream). See `CHANGELOG.md`'s `[1.0.0]`
entry and design spec §10 for the same note.

## Full gate (all steps, same session)

| Step | Command | Result |
|---|---|---|
| Corpus | `npm run test:corpus` | 100 passed + 1 skipped / 1061 passed + 1 skipped |
| Angular #1 | `npm run test:ng` | 74/74 files, 760/760 tests |
| Angular #2 | `npm run test:ng` | 74/74 files, 760/760 tests (identical — no flake) |
| Styles | `npm run build:styles` | Done in 193ms |
| Library | `npx ng build angular-advanced-odontogram` | Built angular-advanced-odontogram (1.5s) |
| Demo | `npm run build:demo` | Application bundle generation complete; `dist/demo` populated |

All green. No behavior changes were made in this task — package.json version
fields, CHANGELOG, spec annotations, and this evidence sheet only.
