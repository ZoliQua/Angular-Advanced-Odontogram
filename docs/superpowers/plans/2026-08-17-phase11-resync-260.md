# Angular Odontogram Port — Phase 11: 2.6.0 Upstream Resync (Diagnosis Coding, Lazy Loading, Rename)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal (owner directive 2026-09-20):** Resync the Angular port to `react-advanced-odontogram` **v2.6.0** (engine commit `215c43a`, payload **2.22**) — 108 upstream commits since the `934a911` pin — and adopt the upstream repository rename (`React-Odontogram-Modul` → **`React-Advanced-Odontogram`**) everywhere. Our version becomes **2.6.0** (semver stays locked to the React module). **NO RELEASE THIS PHASE:** commit and push `main` only. Never create or push a `v*` tag — a tag push triggers the npm publish workflow, and the owner publishes manually.

**Architecture:** Core stays verbatim under the sanctioned deviations, but TWO upstream refactors move where those deviations live and how the library loads:
1. **i18n is now lazy per language.** `i18n/translations.ts` is a test-only aggregator; the strings live in `i18n/locales/<lang>.ts` (12 modules) behind `i18n/loader.ts` (literal `import()` per language, English static). Our branding deviations (app.title, credits.*) now apply to `locales/*.ts`. A guard test fails if any non-test module imports `translations.ts` — the Angular shell must respect that too.
2. **Measured anatomy is lazy** (`anatomy/measured.ts` + `anatomy/profiles.ts`, main chunk −39% upstream). This is the fix for the bundle growth flagged at the end of Phase 7 — the Angular `gen:assets` pipeline must mirror the split so the measured artwork leaves the main bundle.
Plus: `odontogram.ts` is split into `state/*` modules (caseMeta, chart, notify, numbering, payload, perio, perioSettings, pulpApical) with a circular-import guard test; a new `dx/*` diagnosis-coding layer (ICD-10 / BNO-10 / ICD-10-CM / SNOMED) with `DiagnosesCard`, a `CaseDiagnosesModal`, FHIR Condition export **and import**, a published CodeSystem + ValueSets; three real bug fixes that also affect this port (PDF invented "John Doe"/DOB, lower-arch perio tooth flip `mirror XOR rot180`, selection-change performance); new public API `getSelectedTeeth()`.

**Tech Stack:** unchanged at runtime. Upstream added `@playwright/test` as a DEV dependency for browser e2e — **not** copied (our dual-runner stands; record the decision).

**Source pin:** `$ENGINE` = `/Users/Zoli/Sites/DentalQuoteCreator/src/modules/odontogram/engine`, pinned at **`215c43a`**. Read upstream ONLY through pinned blobs: `git -C $ENGINE show 215c43a:<path>` and `git -C $ENGINE diff 934a911..215c43a -- <paths>`. Never read the live tree.

## Global Constraints

- **Core-verbatim rule** with the sanctioned deviation set, re-applied in the NEW file locations:
  1. `?raw` teeth-SVG imports → `generated/teeth-svgs` (now also for the lazy `anatomy/measured.ts` path — keep the laziness).
  2. `src/assets` → `assets` path anchors in copied test infra.
  3. `i18n/useI18n.ts` React hook stripped (bus + `t` kept). The new `loader.ts`/`languages.ts` are framework-free — copy verbatim.
  4. **Branding:** `"app.title"` → `"Angular Advanced Odontogram"` in all 12 `locales/*.ts`.
  5. **PDF footer branding:** app name + repoUrl → the Angular repo.
  6. **Credits adaptations** (Phase 7/9 rulings): `credits.intro` reworded, `credits.originalProjectTitle`/`Desc` added (×12), `credits.welcome` reworded without the "credited here" clause, modal identity (GitHub button, libraries) Angular-side, Contributors section absent. Re-apply ALL of these in `locales/*.ts`, and verify the trimmed modal still renders no contributor list even though upstream's tables now credit downstream forks.
- **Repository rename sweep:** every `React-Odontogram-Modul` URL → `React-Advanced-Odontogram`, in core file headers (comes with the verbatim copy), our 13 READMEs, `projects/angular-advanced-odontogram/README.md`, `package.json` links, credits modal, CHANGELOG, CITATION-style references, the spec, and `.github/workflows` comments. Upstream's own docs also now link back to THIS Angular port — mirror that reciprocal link where our docs list the original project. Grep must end at zero occurrences of the old name outside historical CHANGELOG/plan/acceptance entries.
- **Payload 2.22** is the new round-trip target; regenerated goldens (FHIR, roundtrip, SVG fingerprints, API-surface golden) are adopted from upstream.
- **Bug fixes inherited** (must land, with upstream's own regression tests running): PDF "not specified" instead of invented identity; perio lower-arch flip `mirror XOR rot180` in BOTH anatomy profiles; one-pass control-label gathering. Do not re-derive them — they arrive with the verbatim core; verify each is present and its test runs.
- **Lazy-loading discipline:** after the port, the Angular demo's main bundle must NOT contain the 11 non-English locale tables nor the measured anatomy artwork. Verify with a real build (chunk listing + size deltas vs 2.4.1) and record it. Adjust `angular.json` budgets DOWN if the main bundle shrinks materially; never raise them to mask a regression.
- **Commit policy** (repo-root `CLAUDE.md`, untracked): sole author `Zoltán Dul <zoltan.dul@gmail.com>`, **no Co-Authored-By / AI attribution anywhere**, EMPTY commit bodies, single-line conventional-commit messages, both `GIT_AUTHOR_DATE` and `GIT_COMMITTER_DATE` set (+02:00). Day **2026-08-17**, max 10 commits: #1 09:30 plan, #2 10:30 T1, #3 11:30 T2, #4 13:00 T3, #5 14:15 T4, #6 15:30 T5, #7 16:45 T6, #8 18:00 T7; #9–#10 fix reserve; overflow → 2026-08-18. Fix rounds AMEND in place.
- **Never commit:** `CLAUDE.md`, `.superpowers/`, `.claude/`, `node_modules/`, `dist/`, `docs/api/`, `projects/angular-advanced-odontogram/styles.css`.
- **NO TAG, NO RELEASE.** Push `main` only. The owner runs the release manually.
- Gates at every task boundary: `npm run test:corpus` green; `test:ng` red only per the Task-1 inventory, burned down by T5; library build green.

---

### Task 1: Core + corpus resync to 215c43a

**Files:**
- Modify/Create under `projects/angular-advanced-odontogram/src/lib/core/`: the full pinned `src/` tree minus React files — including the NEW `dx/{codes,caseCodes,derive,packs,refine}.ts`, `state/{caseMeta,chart,notify,numbering,payload,perio,perioSettings,pulpApical}.ts`, `anatomy/{profiles,measured}.ts`, `i18n/{languages,loader,translations}.ts` + `i18n/locales/*.ts` (12), `fhir/{codeSystemResource,importConditions,importPerio,toFhirCase,toFhirDx,valueSetResources}.ts`, changed teeth SVGs (classic + measured), and the FULL `src/__tests__` corpus (215 changed/new files) incl. regenerated goldens.
- Delete: any core file the pin diff deletes (check `git -C $ENGINE diff --name-status 934a911..215c43a -- src | grep '^D'`).
- Modify: `scripts/generate-svg-assets.mjs` (regenerate classic + measured; keep the measured set behind the lazy boundary), `vitest.config.ts` (exclusion audit for the new corpus files, `phase-11` annotations), root + library `package.json` if the pinned runtime deps changed (Playwright is dev-only — exclude, record).

**Interfaces:**
- Produces the v2.6.0 core surface Tasks 2–5 consume. Record in the report the exact new/changed exports (`getSelectedTeeth`, `getDiagnosisCodingPack/setDiagnosisCodingPack`, `getSnomedEnabled/setSnomedEnabled`, `getToothDiagnoses/getActiveDiagnoses`, `setDxOverride`-style setters, `ToothDiagnosis`/`ActiveDiagnoses`/`ActiveDiagnosisRow` types, case-condition API, FHIR import entry points, `anatomy/profiles` API, i18n `loader` API) — Tasks 2–5 work from that list.

- [ ] **Step 1:** Verify `$ENGINE` is at `215c43a`. Re-copy the core set + corpus from pinned blobs; delete what the pin deletes.
- [ ] **Step 2:** Re-apply every sanctioned deviation in its NEW location (branding + credits now in `locales/*.ts` ×12; `?raw` swap in both the classic and the measured import paths; test-infra anchors; `useI18n` hook strip). Confirm the upstream lazy-load guard test still passes with our copies.
- [ ] **Step 3:** `npm run gen:assets` extended for the split (classic eager, measured lazy); verify generated modules compile and the measured module is import()-reachable only.
- [ ] **Step 4:** Diff-discipline check: every copied file diffs empty against its pinned blob except the sanctioned hunks; record the full output and justify each non-empty diff.
- [ ] **Step 5:** Exclusion audit for every new/changed corpus test (framework-free → runs; React-dependent → `REACT_DEPENDENT` with a `phase-11` annotation). Record each decision.
- [ ] **Step 6:** Gates: `npm run test:corpus` GREEN (record counts; baseline 1177/1178); `npx ng build angular-advanced-odontogram` green; `npm run test:ng` run ONCE with every failure INVENTORIED per file with root cause — that inventory is the T2–T5 burn-down contract.
- [ ] **Step 7:** Commit — `feat: resync core and corpus to react-advanced-odontogram 2.6.0`

---

### Task 2: Lazy i18n + lazy anatomy integration

**Files:**
- Modify: `lib/components/odontogram-ui.service.ts` (language switching must await `setI18nLanguage()`-equivalent loading before flipping — mirror how the pinned provider holds its first render), `odontogram-shell.component.ts` (initial language, SSR/async guard), `lib/components/surfaces/odontogram-topbar.component.ts` (language menu), any component reading `t()` at construction time.
- Modify: `lib/testing/reset-engine-state.ts` (loaded-locale cache + anatomy-profile state), `lib/core/generated/*` wiring for the lazy measured artwork.
- Verify: no Angular module statically imports `core/i18n/translations.ts` (mirror upstream's guard as an Angular-side test) and none statically imports `anatomy/measured`.

**Interfaces:**
- Consumes T1's `i18n/loader` + `anatomy/profiles` APIs. Produces the async-language contract T3/T4 components rely on.

- [ ] **Step 1:** Port the async language load; keep `t()` synchronous everywhere; ensure the first paint never shows keys.
- [ ] **Step 2:** Add Angular-side guard specs: (a) no static `translations.ts` import outside tests, (b) switching language loads its chunk then renders, (c) measured anatomy loads only when selected.
- [ ] **Step 3:** Build evidence: `npm run build:demo`, record main-bundle size + the new lazy chunks (11 locales + measured artwork) vs the 2.4.1 numbers; lower `angular.json` budgets if the main bundle genuinely shrank.
- [ ] **Step 4:** Gates: build green; `test:ng` no worse than the T1 inventory; corpus untouched-green.
- [ ] **Step 5:** Commit — `feat: load UI languages and measured anatomy on demand`

---

### Task 3: Diagnosis-coding UI port

**Files:**
- Create: `lib/components/surfaces/cards/diagnoses-card.component.ts` (from pinned `src/surfaces/cards/DiagnosesCard.tsx`), `lib/components/case-diagnoses-modal/case-diagnoses-modal.component.ts` (from pinned `src/CaseDiagnosesModal.tsx`).
- Modify: `tooth-controls-surface.component.ts` (mount the Diagnoses card in its pinned position), `settings-modal.component.ts` + `testing/make-settings.ts` (coding-pack selector: WHO ICD-10 / BNO-10 / ICD-10-CM; SNOMED toggle, default off — exact ids/labels from the pinned Settings delta), the perio case panel host for the case-diagnoses picker, `odontogram-ui.service.ts` (modal open/close state, pack/SNOMED mirrors).

**Interfaces:**
- Consumes T1's `dx/*` API + T2's async-i18n contract. Produces the diagnosis UI surface T5 exports and tests.

- [ ] **Step 1:** Transcribe both components with byte-identical DOM ids/classes/markup; local React state → signals; `useEngineState` → `engineState`; apply the force-value directive to every engine-backed `[value]`/`[checked]` control (Phase-7 parity rule).
- [ ] **Step 2:** Settings wiring + fixture; case-diagnoses picker in the perio panel.
- [ ] **Step 3:** Gates: build green; `test:ng` at or better than the inventory; corpus untouched.
- [ ] **Step 4:** Commit — `feat: port the diagnosis coding UI from 2.6.0`

---

### Task 4: Shell, surfaces, tour and perio deltas

**Files:**
- Modify: `odontogram-ui.service.ts` + `odontogram-shell.component.ts` (pinned `OdontogramContext.tsx`/`App.tsx` deltas: new state, `getSelectedTeeth`, diagnoses wiring, PDF-identity fix surface, availability), the four surface components, `credits-modal.component.ts` (upstream's credits changed — keep our trimmed, Angular-identity version; adopt only non-contributor structural changes), perio components (`perio-chart`, `perio-grid-dom`, `perio-sidebar`: the `mirror XOR rot180` lower-arch flip lands in core, but verify the Angular grid consumes it correctly in BOTH profiles), the guided tour (18 steps — verify every target id exists in our DOM).

- [ ] **Step 1:** Port the deltas region-by-region against the pinned diffs; extend `MUST_HAVE_IDS`.
- [ ] **Step 2:** Verify the three inherited bug fixes behave in the Angular shell (PDF with an empty case prints "not specified" and no invented age; lower-arch perio orientation correct in classic AND measured; selection click path does one label pass).
- [ ] **Step 3:** Gates: build green; `test:ng` within inventory; corpus untouched.
- [ ] **Step 4:** Commit — `feat: port shell, tour and periodontal deltas from 2.6.0`

---

### Task 5: Test burn-down + public API

**Files:**
- Modify: every red ported spec per the T1 inventory (re-diff each against its updated corpus source; port deltas, drift-noted), new ported specs for React-dependent new corpus files (annotate `ported:`/`covered-by:`/`react-only:` — no unannotated `phase-11` entries left), `lib/testing/reset-engine-state.ts` (every new singleton: dx overrides, case conditions, coding pack, SNOMED flag, loaded locales, anatomy profile), `public-api.ts` (export the new core API + `DiagnosesCardComponent`, `CaseDiagnosesModalComponent`, and every type upstream's entry point exposes).

- [ ] **Step 1:** Burn the inventory to zero; extend reset; sweep exports against the pinned `index.ts`/`App.tsx` export lists.
- [ ] **Step 2:** Gates: `npm run test:corpus` green && `npm run test:ng` ×2 identical green && lib build && `npm run build:demo`.
- [ ] **Step 3:** Commit — `test: burn down the 2.6.0 resync suite and export the diagnosis API`

---

### Task 6: Repository rename sweep + documentation refresh

**Files:**
- Modify: `README.md` + all 12 `lang/README-*.md` (rename sweep; mirror upstream's 2.5.0/2.6.0 README pass: diagnosis-coding section, export-format reference for `dxOverrides`/`caseConditions` at payload 2.22, lazy-loading notes, the reciprocal Angular/React cross-link, version 2.6.0 badges), `projects/angular-advanced-odontogram/README.md` (npm-facing: rename + new feature bullets + both original-project links), `projects/angular-advanced-odontogram/package.json` (repository/bugs/homepage links unchanged for OUR repo — only the *original project* references in text change), `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, `.github/workflows/*` comments, `docs/superpowers/specs/2026-08-06-angular-odontogram-port-design.md` (§2 deviation set in its new locations, §8 Phase 11 delivered, §10 drift → 215c43a).
- Verify: `grep -rn "React-Odontogram-Modul"` returns only historical CHANGELOG/plan/acceptance lines.

- [ ] **Step 1:** Rename sweep + content refresh; EN canonical first, then the 11 translations 1:1 (localized labels from `locales/*.ts`, all table headers translated, closing sentence present — the Phase-8 lesson list).
- [ ] **Step 2:** Gates: link/anchor check across all 13 docs (record method); docs-only diff; lib build once.
- [ ] **Step 3:** Commit — `docs: adopt the React Advanced Odontogram rename and refresh all 13 documents`

---

### Task 7: 2.6.0 version cut (no release)

**Files:**
- Modify: root + library `package.json` → 2.6.0 (+ lockfile), `lib/core/app-version.ts` LIB_VERSION, 13 README badges, `CHANGELOG.md` (`## [2.6.0] - 2026-08-17`: diagnosis coding layer, FHIR Conditions + perio import, CodeSystem/ValueSets, lazy i18n + lazy anatomy with the measured bundle numbers, the three inherited bug fixes, `getSelectedTeeth`, the repository rename; compare links `v2.4.1...v2.6.0`).
- Create: `docs/superpowers/specs/2.6.0-acceptance.md` (evidence discipline: real suite runs, `npm pack --dry-run`, consumer resolution proof, demo bundle + lazy-chunk table vs 2.4.1, 13-file badge/link grep).

- [ ] **Step 1:** Cut + FULL GATE: `npm run test:corpus` && `npm run test:ng` ×2 && `npm run build:styles` && lib build && `npm run build:demo`.
- [ ] **Step 2:** Commit — `release: 2.6.0 — diagnosis coding, on-demand loading and the upstream rename`

---

## Close-out (controller)

Final whole-branch review (most capable model, range = plan commit..T7) → fix wave if needed → merge (`git update-ref refs/heads/main`) → worktree removal → parent reset → **push `main` only** → browser smoke (Diagnoses card, case-diagnoses pop-up, coding-pack + SNOMED settings, language switch chunk load, measured anatomy, credits) → **STOP: no tag, no GitHub release, no npm publish** — hand the release to the owner with the exact steps.
